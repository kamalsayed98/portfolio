Place your profile photo here as `profile.jpg` (or update `personal.photo` in `js/portfolio-data.js` to match a different filename).

Recommended: a square or slightly portrait-oriented headshot, at least 720px on the short side.
